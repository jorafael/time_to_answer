<h1>Site::Welcome#index</h1>
<p>Find me in app/views/site/welcome/index.html.erb</p>

<% if user_signed_in? %>
  <p> Usuário ESTÁ logado </p>
  <p> <%= current_user.email %> </p>
<% else %>
  <p> Usuário NÃO ESTÁ logado (<%= link_to "Logar", admins_backoffice_welcome_index_path %>)</p>
<% end %>

<% if admin_signed_in? %>
  <p> Administrador ESTÁ logado </p>
  <p> <%= current_admin.email %> </p>
<% else %>
  <p> Administrador NÃO ESTÁ logado (<%= link_to "Logar", users_backoffice_welcome_index_path %>)</p>
<% end %>